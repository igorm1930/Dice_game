# Build artifacts — formatting compiled output is noise, and checking it would
# make format:check depend on whether a build has run.
node_modules/
dist/
build/
coverage/
.next/
.turbo/
pnpm-lock.yaml
test-results/
playwright-report/
