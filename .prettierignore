# Build artifacts — formatting compiled output is noise, and checking it would
# make format:check depend on whether a build has run.
node_modules/
dist/
build/
coverage/
.next/
.turbo/
pnpm-lock.yaml
package-lock.json
test-results/
playwright-report/

# The legacy Express implementation, retained until the domain is ported in
# Phase 2. Nothing new should be added here.
src/
tests/
client/
