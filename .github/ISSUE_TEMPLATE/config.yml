# A blank issue is an issue with no reproduction steps and no context, which
# costs a round trip before it can be read. Both templates are short enough that
# picking one is cheaper than that round trip.
blank_issues_enabled: false
